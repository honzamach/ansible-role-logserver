---

hm_logserver__install_packages:
  debian:
    apt:
      - syslog-ng
      - stunnel

hm_logserver__host_log_dir: /var/log/cls-servers

hm_logserver__log_all_file: /var/log/net-all.log

hm_logserver__compress_older_than: 31

hm_logserver__cleanup_older_than: 1095
